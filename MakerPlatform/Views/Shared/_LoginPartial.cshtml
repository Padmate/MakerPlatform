@using Microsoft.AspNet.Identity

<style>
    .login-li-bg {
        background-color:transparent;
    }
    .login-li-font {
        color: #fed136;
    }
    .login-box
    {
        margin-left:50px;
    }
</style>
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav navbar-right login-box">
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" style="color: #8ece1f; background-color: transparent;">
                @User.Identity.GetUserName()
                <b class="caret"></b>
            </a>
            <ul class="dropdown-menu login-li-bg">
                <li>
                    <a href="/Atricle/Index?type=activity">活动管理</a>
                </li>
                <li>
                    <a href="/Atricle/Index?type=information">资讯管理</a>

                </li>
                <li>
                    @Html.ActionLink("密码管理", "Manage", "Account")
                </li>
                <li><a href="javascript:document.getElementById('logoutForm').submit()" >注销</a></li>
            </ul>
        </li>

    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right login-box">
        <li>@Html.ActionLink("登录", "Login", "Account", new { ReturnUrl = Request.Url.PathAndQuery }, htmlAttributes: new { id = "loginLink", style = "color: #8ece1f;" })</li>
    </ul>
}
